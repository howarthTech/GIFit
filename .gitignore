*.iml
.gradle
/local.properties
/.idea
/build
/app/build
.DS_Store
/captures
.externalNativeBuild
.cxx
local.properties
*.hprof

# Release signing (keep secret)
keystore.properties
*.jks
*.keystore

# Built artifacts kept in the project folder for sideloading/upload (synced via
# Drive) — deliverables live in dist/, but never get committed to the repo.
*.apk
*.aab
/dist/
